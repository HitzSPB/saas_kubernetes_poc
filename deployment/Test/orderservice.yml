apiVersion: v1
kind: Service
metadata:
  name: orderservice-poc
  labels:
    app: orderservice-poc
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: orderservice-poc
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: orderservice-poc
  labels:
    app: orderservice-poc
spec:
  replicas: 10
  selector:
    matchLabels:
      app: orderservice-poc
  template:
    metadata:
      labels:
        app: orderservice-poc
    spec:
      containers:
      - name: orderservice
        image: hitzspb/orderservice:latest
        resources:
          requests:
            memory: "300Mi"
            cpu: "150m"
          limits:
            memory: "300Mi"
            cpu: "200m"
        ports:
           - containerPort: 80
           